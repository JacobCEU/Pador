const payment_model = (bank_id,card_type)=>{

    let Appoint = {
        bank_id : bank_id,
        card_type : card_type,
    }
    
    return Appoint
    
    }
    module.exports = {
        payment_model
    }